{
  "name": "photos",
  "private": true,
  "scripts": {
    "start": "open http://localhost:3081 && next dev -p 3081",
    "build": "next build && next export -o build/",
    "lint": "next lint"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "next": "12.0.4",
    "next-transpile-modules": "^9.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "styled-components": "^5.3.3"
  }
}
